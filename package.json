{
  "name": "devhatch",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "npm@11.19.0",
  "engines": {
    "node": ">=26",
    "npm": ">=11.19"
  },
  "scripts": {
    "dev": "concurrently -k -n build,server \"vite build --watch --config apps/web/vite.config.ts\" \"cargo run -p devhatch-server\"",
    "prestart": "npm run build:web",
    "start": "cargo run -p devhatch-server --release --locked",
    "typecheck": "tsc -b apps/web/tsconfig.json",
    "build:web": "tsc -b apps/web/tsconfig.json && vite build --config apps/web/vite.config.ts",
    "build:server": "cargo build --workspace --release --locked",
    "build": "npm run build:web && npm run build:server",
    "lint": "oxlint apps/web/src apps/web/vite.config.ts",
    "test": "vitest run --config apps/web/vite.config.ts",
    "preview": "npm start"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "fuse.js": "^7.5.0",
    "lucide-react": "^1.31.0",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.4",
    "concurrently": "^10.0.5",
    "oxlint": "^1.75.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.0.0"
  }
}
